# Sessions

Cette arborescence contient les sessions `001` à `102`.

Chaque session suit la structure suivante :

- `gold/session/<session_id>/m1/`
- `gold/session/<session_id>/m15/`

Les dossiers vides sont conservés dans Git grâce à des fichiers `.gitkeep`.
